Boolean ist ein Werttyp, der nur
`True` oder `False` sein kann.
Der Operator `==` (Gleichheit) prüft,
ob die beiden zu vergleichenden
Variablen gleich sind.

Prüfe, ob die Variable
`two` gleich `three` ist.
